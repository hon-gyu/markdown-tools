---
title: Postgres Indexes
tags: [postgres, databases]
---

# Postgres Indexes

`postgres/` has no `index.md`, so in the sidebar it shows as a plain grouping
label (`Postgres/`) rather than a link — a folder without a page of its own.
